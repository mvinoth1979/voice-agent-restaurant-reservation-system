{
  "name": "voice-agent-restaurant-reservation-system",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix backend run build",
    "start": "npm --prefix backend run start"
  }
}
